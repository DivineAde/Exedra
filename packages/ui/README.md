# @whiteboard/ui

Scaffolded for shared design-system primitives. Currently `frontend/components/ui`
holds the shadcn/ui-pattern components (Button, Input, Dialog, Select, ...)
directly, since `frontend` is the only consumer. Promote components here if
a second app needs them.
