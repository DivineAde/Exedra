// Reserved for design-system primitives shared across future apps.
//
// The current shadcn/ui-pattern primitives (Button, Input, Dialog, etc.)
// live in `frontend/components/ui` since this project only has a single
// consumer today. If a second frontend is ever added, extract those
// components here so both apps share one design system.
export {};
