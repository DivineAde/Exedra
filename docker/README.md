# docker/

Local development uses the single `docker-compose.yml` at the repo root
(Postgres + Redis). This folder is reserved for future service-specific
Dockerfiles (e.g. `docker/backend.Dockerfile`, `docker/frontend.Dockerfile`)
once the app is containerized for deployment.
